# Stateless HackerNews clone (with GraphQL, Django, Graphene, JWT, Relay)

- creation of users
- creation of links
- voting on links
- query of users and links with/without use of Relay
- authentication of users with JSON Web Token (JWT)
